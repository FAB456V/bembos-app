{
  "name": "bembos-orders-service",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node index.js",
    "check": "node --check index.js && node --check models/Order.js && node --check controllers/ordersController.js && node --check routes/ordersRoutes.js && node --check middlewares/verifyToken.js && node --check middlewares/verifyServiceKey.js && node --check controllers/iotController.js && node --check middlewares/verifyDeviceKey.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.15.1"
  }
}
